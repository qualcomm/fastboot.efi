[package]
name = "fastboot"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
ffi = "0.1.1"
log = "0.4"
uefi = { version = "0.35", features = ["alloc", "global_allocator", "logger", "panic_handler"] }
uefi-raw = "0.11.0"
uguid = "2.2.0"
